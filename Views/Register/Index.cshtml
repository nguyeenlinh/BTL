@* @model IEnumerable<BTL_NET2.Models.Register>

    @{
    ViewData["Title"] = "Index";
    }

    <h1>Index</h1>

    <p>
    <a asp-action="Create">Create New</a>
    </p>
    <table class="table">
    <thead>
    <tr>
    <th>
    @Html.DisplayNameFor(model => model.RegisterName)
    </th>
    <th>
    @Html.DisplayNameFor(model => model.RegisterMK)
    </th>
    <th></th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model) {
    <tr>
    <td>
    @Html.DisplayFor(modelItem => item.RegisterName)
    </td>
    <td>
    @Html.DisplayFor(modelItem => item.RegisterMK)
    </td>
    <td>
    <a asp-action="Edit" asp-route-id="@item.RegisterID">Edit</a> |
    <a asp-action="Details" asp-route-id="@item.RegisterID">Details</a> |
    <a asp-action="Delete" asp-route-id="@item.RegisterID">Delete</a>
    </td>
    </tr>
    }
    </tbody>
    </table> *@
